#include <stdio.h>#include <string.h>#define MAX_USERNAME_LENGTH 50#define MAX_PASSWORD_LENGTH 50#define MAX_USERS 100typedef struct {    char user_name[MAX_USERNAME_LENGTH];    char password[MAX_PASSWORD_LENGTH];} User;User users[MAX_USERS];int num_users = 0;void registeration (const char* user_name, const char* password) {    if (num_users < MAX_USERS) {        strcpy(users[num_users].user_name, user_name);        strcpy(users[num_users].password, password);        num_users++;        printf("User registered successfully \n");    } }int login(const char* user_name, const char* password) {    for (int i = 0; i < num_users; i++) {        if (strcmp(users[i].user_name, user_name) == 0 &&            strcmp(users[i].password, password) == 0) {            return i;          }    }    return -1; }int main() {    char user_name[MAX_USERNAME_LENGTH];    char password[MAX_PASSWORD_LENGTH];    printf("Enroll Now \n");    printf("Enter username :: ");    scanf("%s", user_name);    printf("Enter password :: ");    scanf("%s", password);    registeration(user_name, password);    printf("\nLogin\n");    printf("Enter username :: ");    scanf("%s", user_name);    printf(" Enter password :: ");    scanf("%s", password);    int UserIndex = login(user_name, password);    if (UserIndex != -1) {        printf("Login successful  &&  User index: %d\n", UserIndex);    } else {        printf("Invalid Data !! \n");    }    return 0;}